# ============================================
# Що НЕ заливається на GitHub
# ============================================

# ДЖЕРЕЛА АРТУ (103 МБ) — ШІ-генерації до обробки і файли Photopea.
# Грі вони не потрібні: у гру йдуть уже оброблені спрайти з assets/sprites/.
# ⚠️ Це означає, що джерела живуть ТІЛЬКИ на твоєму комп'ютері —
# тримай архів (StudLife_sources_backup.zip) десь у хмарі.
assets/source/
*.psd

# архів джерел — не заливаємо, він великий
StudLife_sources_backup.zip

# сміття операційних систем
Thumbs.db
desktop.ini
.DS_Store

# тимчасові файли редакторів
*.tmp
*.bak
*~
.vscode/
.idea/
