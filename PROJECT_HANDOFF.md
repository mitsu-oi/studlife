# 📦 PROJECT HANDOFF — «Складнощі студентського життя»

> Цей файл — повна передача контексту проєкту для будь-якого ШІ-асистента або
> розробника. Прочитавши його (і три документи, на які він посилається),
> можна продовжувати роботу з того місця, де ми зупинились.
> Оновлено: 2026-07-17.

---

## 1. Що це за проєкт

**Браузерна RPG-виживалка** про будні студента Львівської політехніки
(у грі — просто «Політех»), який живе у гуртожитку №11 («Одинадцятка»)
і має протриматись **30 днів до стипендії**, балансуючи 5 шкал:
💰 Гроші · ⚡ Енергія · 🧠 Менталочка · 👥 Соціалка · 📚 Навчання.

Авторка — **Даша**, це її ПЕРША гра. Гумор — добрий, правдивий, на основі
реальних деталей Політеху й «Одинадцятки» (вона там живе/жила — інсайдерка).

**Ключові документи (в корені проєкту):**
- `GAME_DESIGN.md` — повний геймдизайн: шкали, події, форс-мажори, кінцівки, сатира
- `ART_DESIGN.md` — арт-стиль, палітра, персонажі, УСІ промпти для генерації
- `ROADMAP.md` — 22 етапи розробки з чеклістами (що зроблено — відмічено)

## 2. Технології і принципи

- **Чистий HTML + CSS + JavaScript, без рушіїв і збірок.** Гра відкривається
  подвійним кліком по `index.html`. Це принципово: Даша вчиться, кожен рядок
  має бути зрозумілим.
- **Мова:** усі тексти гри й коментарі коду — українською; імена змінних/функцій/файлів — англійською.
- Сцена — Canvas 2D у **нативній роздільності фонового арту 1672×941**
  (жодних стискань: якість = якість джерела; CSS `image-rendering: auto`).
- Контент — у `.js`-файлах (`const DATA = [...]`), НЕ `.json`, бо `file://`
  блокує fetch. Структури дзеркалять майбутні таблиці БД.
- Збереження — `localStorage`, автосейв щоранку.
- **Майбутнє (Фази 2–4):** кілька універів → Spring Boot + PostgreSQL
  (схема вже готова: `university_game_schema.sql`, схеми core+rating) →
  PWA з push-сповіщеннями (тамагочі-режим). Архітектура це вже враховує.

## 3. Структура файлів

```
index.html            — каркас: HUD / канвас / зона рішень / оверлей вікон
css/style.css         — все оформлення (HUD, картки, календарик, вікна, чит-панель)
js/state.js           — СТАН: gameState, CONFIG, шкали, хід часу, сейви, кінцівки
js/events.js          — ДВИГУН КАРТОК: відбір за умовами, вибір, ефекти
js/characters.js      — 3 персонажі: рутини, ходьба, анімації, сон
js/boris.js           — тарган Борис: маршрути по стінах/стелі/підлозі, смерть від кліку
js/props.js           — предметні шари станів: 💰/📚/🧠 вмикають предмети в кімнаті
js/npc.js             — сусіди: клік → діалог → квести з відкладеною нагородою
js/ai.js              — ШІ-КАРТКИ: запас карток наперед, пам'ять тем і виборів,
                        фолбек на data/events.js (сервер — у server/worker.js).
                        Стан ШІ видно в чит-панелі D (працює / ліміт / помилка)
server/worker.js      — «поштова скринька» на Cloudflare: ховає ключ, пише
                        картки (Gemini). server/README.md — інструкція для Даші
js/minigames.js       — МІНІГРИ: клікабельні куплені предмети (іконка+сяйво над
                        ними), 🪳 аркада «Борис біжить», 🖨️ мікробізнес принтера
data/printer.js       — події принтера: замовлення (заробіток) і проблеми
data/events.js        — КОНТЕНТ карток; data/quests.js — сусіди+квести (Етап 8);
                        data/force_majeure.js — форс-мажори (Етап 9)
js/scene.js           — рендер: фон → персонажі → оклюдери → Борис → світло
js/ui.js              — HUD, картки, нічний підсумок, календарик, фінал, чит-панель
js/main.js            — ініціалізація, цикл rAF, обробка помилок (показ у HUD)
data/events.js        — КОНТЕНТ: ранкові вибори + ~11 карток подій
assets/sprites/       — ігрові асети: room_base.png, characters/*.png (стрипи), overlays/
assets/source/        — джерела-оригінали (ШІ-генерації до обробки)
assets/reference/     — mood_target.png і dark_ref.png (еталони вигляду)
tools/process_chars.html — авто-нарізчик аркушів персонажів (детально нижче)
tools/asset_processor.html — ручний обробник картинок (даунскейл+квантизація)
tools/process_room.html  — (історичний, для фону більше не потрібен)
```

## 4. Ігровий цикл (що вже грається)

Стартовий екран (Продовжити/Нова гра з підтвердженням) → **вступ-знайомство**
(нова гра: локація «Одинадцятка» → вибір ІМЕНІ → сусіди Остап-«Ботан» і
Дєнчик-«Тусовщик» представляються з гумором і живою мовою → «Заселитися»;
ui.js showIntro/INTRO_SLIDES, структура готова прийняти вибір універу/общаги
у Фазі 2) → **календарик**
(настінний, минулі дні закреслені ✗) → день з 4 фаз:
**ранок** (фіксований вибір: пари/робота/відіспатись; вихідні — свій) →
**день** і **вечір** (випадкова картка з пулу: текст → 2-3 кнопки → наслідок
з плашками змін «⚡ −12») → **ніч** («Спати» → підсумок дня → новий день).
Вихідні: +15⚡, свої картки. Гроші САМІ НЕ КАПАЮТЬ: тільки вибір «робота»
(+300₴), мамин переказ (рідкісна картка), борг у Ботана (раз за гру, при
💰≤50, у фіналі −600) і стипендія (фінал, якщо 📚≥60: +2000₴).

**Програш:** ⚡=0 (госпіталізація), 🧠=0 (нервовий зрив), 📚=0 (відрахування);
екран з кнопкою «⟲ Переграти цей день» (сейв ранковий). **Фінал (день 30):**
4 кінцівки (champion/burned/happy_poor/classic) + статистика місяця
(пар відвідано, разів проспав, Бориса помічено/прибито…).

**Формат картки** (data/events.js): `{ id, phases, weekend?, weight?,
cooldownDays?, minStats?, maxStats?, oncePerGame?, text, choices: [{ label,
effects: {stat:±N}, result, requires?: {stat, gte}, roll?: [{p, effects,
result}], setFlag?, count? }] }` — двигун усе це підтримує.

## 5. Жива кімната (найбільша гордість)

**Персонажі** (js/characters.js) — Гравець (КН, худі), Ботан (прикладна
математика, светр+окуляри), Тусовщик (менеджмент, чорний рокер-лук):
- **рутини**: сидить за справою (dwell ~5с) → встає → ІДЕ (4-кадровий цикл
  кроку, кадри прив'язані до пройденої відстані — 36 px/кадр, ноги не
  ковзають) → інша справа. Тусовщик на верхній ярус «лізе» біля драбини (фейд).
- **анімації на місці**: Тусовщик грає на гітарі (2 кадри руки; ноги
  «заморожені» через lockLegsY — низ завжди з 1-го кадру, складання на
  службовому канвасі) + нотки ♪ кодом; гортає телефон (2 кадри); Ботан читає
  біля полиць (2 кадри), пише конспект сидячи, нотує стоячи.
- **дихання** на місці — кодом (scale Y 1±0.009), НЕ кадрами (idle-аркуші
  пробували — Даші не сподобались, викинули).
- **сон**: у нічну фазу всі йдуть до СВОЇХ ліжок і лягають (конфіг sleep
  {x,y,pose,rot,flip}, якір-центр, «Z-z» кодом); вранці прокидаються.
- **тіні** під ногами — еліпси кодом.

**Оклюдери** (scene.js): прямокутні шматки фону поверх персонажів (столи,
ноут) — сидячи, ноги ховаються за стільницею. + Накладки з прозорістю
(ріже Даша в Photopea з room_base_source):
`overlays/chairs.png` — нічна (ковдри/бортики; з'являється фейдом, щойно
ХОЧ ХТОСЬ ліг спати — CHARACTERS.some(sleep)), `overlays/always.png` — постійна (опційна),
`overlays/mess.png` — ШАР МУСОРУ на всю кімнату (пляшки/обгортки/одяг/плями):
з'являється плавним фейдом при 🧠 ≤ MESS_MENTAL_MAX (=30) АБО ⚡ ≤
MESS_ENERGY_MAX (=30) — обидва пороги у scene.js; зникає при відновленні
шкал; малюється ПІД персонажами (весь мотлох на задній план — герої ходять
поверх нього). Оклюдери столів домальовують свій шматок мотлоху, щоб не
затирати сміття чистим фоном.

**Борис 🪳** (js/boris.js) — кодогенерований тарган: вилазить з-за реальних
меблів, повзе (стіна=вид зверху зменшений ×0.72 / стеля=профіль догори
лапками / підлога=профіль), ховається за меблі (маска-cover через
ctx.clip evenodd — заповзає поступово). Клік по ньому = тапок: зі
стіни/стелі падає з обертанням, лежить лапками догори, дригається, зникає.
Лічильник у фінал. При 🧠<30 з'являється в 2.5 рази частіше.

**Світло** (scene.js, renderLighting) — ЗАЛІЗНЕ ПРАВИЛО: світло ніколи не
запікається в арт, тільки код: ранок/день — тепле світло з вікна праворуч;
вечір — люстра (тепла пляма); ніч — темрява + місяць з вікна; 🧠<30 —
тьмяно/холодно/віньєтка густіша; 🧠≥70 — тепліше. Плавні градієнти з
розмиттям, БЕЗ дизерингу (Даша: «дизеринг-світло — як плівка»).

## 6. Арт-пайплайн (як робиться графіка)

Все генерує ШІ (Даша), обробляють інструменти, оживляє код. Промпти — в ART_DESIGN.md.

**Фон кімнати**: генерація 16:9 (зараз 1672×941) з РІВНИМ нейтральним світлом
(без плям/променів!) → у гру ЯК Є (без ресемплінгу — ресемплінг убиває
деталі, Даша це ненавидить). Даша сама доредаговує фон у Photopea
(олівець, PNG, розмір не міняти).

**Персонажі**: аркуш = 1 генерація з N позами в ряд на МАДЖЕНТА-фоні
(#FF00FF), без тіней і світла → `tools/process_chars.html` автоматично:
хромакей (r>60 && b>60 && g<min(r,b)*0.55) → розумне зрізання чорних
letterbox-смуг (рядок >90% темний) → пошук поз флуд-філом («острови»,
MIN_AREA 20000 відсіює вотермарки) → ФІЛЬТР ЧОРНИХ СИЛУЕТІВ (середня
яскравість <40 — генератор кладе тіньові копії) → нормалізація зросту
per-аркуш (tallestH у SOURCES) → пакування в стрип комірок 300×344,
низ по центру → headless-скріншот з прозорістю
(`--default-background-color=00000000`).
Запуск: `chrome --headless --allow-file-access-from-files
--window-size=4800,344 --screenshot=... process_chars.html#main|botan|party`.
SOURCES у файлі інструмента — список аркушів на персонажа; новий аркуш =
один рядок там + номери кадрів у characters.js.

**Поточні стрипи**: main: 0-3 базові · 4-8 хода (5 кадрів; ПОРЯДОК циклу не
підряд — [7,6,8,4,5], stepPx 29, див. walkFrames у characters.js; порядок
підібраний так, щоб опорна нога від'їжджала назад — не «місячна хода»); botan:
0-3 · 4-7 хода · 8-9 читає · 10-11 нотує стоячи · 12-13 пише сидячи;
party: 0-3 · 4-7 хода · 8-9 гітара · 10-12 телефон (кадр 11 забракований).

## 7. Тест-режими (хеші до index.html)

`#boris` (тарган одразу, зони по колу) · `#boris-N` (зона 0-3) · `#boris-dead`
(авто-тапок) · `#boris-geo` (РЕДАКТОР геометрії таргана: зони/укриття/координати
під курсором) · `#chars-geo` (РЕДАКТОР місць персонажів: тягни точки і
сплячих мишкою, координати на екрані) · `#props-geo` (РЕДАКТОР предметів
станів: тягни мишкою, колесо = масштаб; без спрайта — пунктирна рамка) · `#calendar-N` (календарик дня N) ·
`#card-<id>` (форсувати картку) · `#card-<id>-N` (і одразу обрати варіант N) ·
`#fm-<id>` (форсувати форс-мажор, напр. #fm-pipe_burst) ·
`#finale` (фінальний екран) · `#gameover-energy/-mental/-study` ·
`#shop` (магазин одразу, з 5000₴) · `#ai` (ШІ-картка одразу) ·
`#boris-run` (аркада про Бориса) · `#xbox` (меню аркад) ·
`#phase-morning/-day/-evening/-night` (фаза одразу) · клавіша **D** —
чит-панель (повзунки шкал + фази + кнопка **🛒 Магазин**: відкриває шопінг
будь-коли, оминаючи поріг грошей і кулдаун; закриття не просуває хід гри).
Помилки JS показуються червоним у HUD.

## 8. Що ЗРОБЛЕНО / що ДАЛІ

✅ Етапи 0–9 (крім емоційних поз): скелет, час+календарик, картки, сейви,
кінцівки, фазове світло, сон, персонажі, Борис, шар мусору, СУСІДИ+КВЕСТИ,
ФОРС-МАЖОРИ.

**Етап 8 (сусіди):** клік по Ботану/Тусовщику → діалог (js/npc.js, ui.js
showDialog). Квести з ВІДКЛАДЕНОЮ нагородою (data/quests.js): взяв → миттєві
ефекти → через N днів підійшов → нагорода. Стан у flags.quests. При 👥<30
сусід відмахується. Відмова ховає квест на 19 днів (QUEST_RETRY_DAYS), потім
може зринути знову. Вранці тост, коли нагорода дозріла. questsDone у фінал.

**Етап 9 (форс-мажори):** data/force_majeure.js — 4 термінові події (труба,
інтернет, таргани, комендант), у кожної emoji+title. Показуються ОКРЕМИМ
модальним вікном ПО ЦЕНТРУ (ui.js showForceMajeure/onFMChoice/showForceMajeureResult;
CSS .window.fm — пульсуюча рамка, велика тематична іконка, бейдж «ТЕРМІНОВО»),
а не в зоні рішень унизу. Двигун у js/events.js: pickForceMajeure (шанс+обмеження
частоти, у enterPhase перед звичайною карткою), applyChoice (спільна логіка вибору
для карток і FM), applyLastingForceMajeure (тривалий drain щоранку в startNewDay),
стан у flags.fm/fmLog. Варіант із resolves:true усуває проблему; інакше тече далі.
Сліди кодом у scene.js (калюжа, Wi-Fi ✕). ЕМОЦІЇ мешканців (characters.js):
поки FM активний — бульбашка над головою (fm.emotion: 😱/😠/😰) + тремтіння/
погойдування тіла; +5с після вирішення (EMOTION_REACTION_MS). Частота FM:
мінімум 2 дні між (FM_MIN_GAP_DAYS) + ≤2/тиждень, з дня 4+. Окремі арт-пози
(замість бульбашок) — опційно на майбутнє.

⬜ Далі за планом (ROADMAP.md):
1. **Емоційні пози** (хвіст Етапу 9): аркуші наляканих/злих поз → пайплайн
   персонажів + бульбашки 😱💢 кодом під час активного форс-мажору
   персонажів (налякані/злі — промпти в ART_DESIGN)
3. Решта Етапу 6: предметні шари станів — СИСТЕМА ГОТОВА (js/props.js:
   умови по шкалах, фейд, редактор #props-geo); лишились тільки генерації
   спрайтів (промпти в ART_DESIGN) → PNG в assets/sprites/props/<id>.png,
   предмет підхопиться сам
4. **Етап 10**: контент до 30–40 карток + баланс (числа зараз плейсхолдери)
5. Етапи 11-12: арт-поліш, реліз на GitHub Pages

## 9. Як працювати з Дашею (важливо для ШІ-асистента)

- Пояснюй просто — це її перша гра, але вона ШВИДКО вчиться: сама редагує
  конфіги (координати, оклюдери, dwell), ріже арт у Photopea, генерує аркуші.
- Вона ЛЮБИТЬ візуальні редактори (#chars-geo, #boris-geo, чит-панель) —
  для нових систем роби такі самі.
- У неї гострий смак: «крінжові» назви-каламбури — ні; мильні/криві
  спрайти — ні; краще менше кадрів, але чисто. Слухай її вердикти щодо
  вигляду — вони фінальні.
- Правки роби маленькими кроками з перевіркою скріншотом (headless chrome).
- Числа балансу і координати — це ЇЇ пісочниця: підписуй їх коментарями.
- Все важливе фіксуй у GAME_DESIGN / ART_DESIGN / ROADMAP — вони живі документи.
